# 站点总览

本页面是 CLI 对比站的文字入口，和 `docs/index.html` 可视化页面保持同一信息口径。

## 覆盖范围

- 对比对象：Claude Code / Codex / OpenCode / Kimi
- 内容模块：配置体系、执行与权限、模型与上下文、迁移路径、场景推荐
- 证据机制：引用上标 + 结构化来源注册表

## 阅读建议

1. 先看 `config-comparison.md` 了解完整维度差异
2. 再看 `migration-quick-reference.md` 定义迁移策略
3. 通过 `migration-examples.md` 套用到真实工作流
4. 最后结合 `methodology.md` 理解验证状态

## 当前状态

- 可视化页面已接入 `data-claim-id` 与自动引用注入
- 已有引用一致性检查与 CI
- 冲突与未验证项仍保留并显式标记，等待后续持续核验
